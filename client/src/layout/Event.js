import ListCard from "../components/eventPage/ListCard"

export default function Event(){
    return(
        <main>
            <section>
            <ListCard />
            </section>

        </main>
    )
}